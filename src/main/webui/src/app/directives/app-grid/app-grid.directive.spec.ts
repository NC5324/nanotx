import { AppGridDirective } from './app-grid.directive';

describe('AppGridDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGridDirective();
    expect(directive).toBeTruthy();
  });
});
